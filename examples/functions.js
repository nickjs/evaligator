// hint: to test the function input parameters, click on the
// annotation on the line number of the function definition
// and assign a value to each parameter

var f = function(inputValueOne, inputValueTwo) {
  var a = inputValueOne + inputValueTwo;
};

var g = function(x,y) {
  var c = x + y;
  return c;
};

var b = g(3,7);
